const menuItems = [{id: 1, name: '牛肉麵', description: '精選牛腱肉100%純牛肉湯', price: 180, emoji: '🍜', category: 'main'}, {id: 2, name: '排骨飯', description: '香酥多汁排骨便當', price: 200, emoji: '🍱', category: 'main'}, {id: 3, name: '雞腿飯', description: '香嫩多汁雞腿便當', price: 220, emoji: '🍗', category: 'main'}, {id: 4, name: '豬腳飯', description: '滷到入味的豬腳12小時慢燉', price: 250, emoji: '🍖', category: 'main'}, {id: 5, name: '炒飯', description: '蛋香四溢特製炒飯', price: 280, emoji: '🍚', category: 'main'}, {id: 6, name: '海鮮麵', description: '新鮮海味的海鮮麵12種海鮮', price: 350, emoji: '🦞', category: 'main'}, {id: 7, name: '珍珠奶茶', description: '招牌Q彈珍珠奶茶', price: 60, emoji: '🧃', category: 'drink'}, {id: 8, name: '綠茶', description: '清爽回甘茉莉綠茶100%茶葉', price: 80, emoji: '🍵', category: 'drink'}, {id: 9, name: '咖啡', description: '香醇咖啡提神首選', price: 90, emoji: '☕', category: 'drink'}, {id: 10, name: '果汁', description: '新鮮現榨果汁', price: 100, emoji: '🧃', category: 'drink'}, {id: 11, name: '布丁', description: '滑嫩香甜焦糖布丁', price: 120, emoji: '🍮', category: 'dessert'}, {id: 12, name: '蛋糕', description: '精緻手工蛋糕', price: 130, emoji: '🍰', category: 'dessert'}, {id: 13, name: '冰淇淋', description: '香濃冰淇淋', price: 90, emoji: '🍦', category: 'dessert'}, {id: 14, name: '餅乾', description: '酥脆可口手工餅乾', price: 50, emoji: '🍪', category: 'dessert'}]; let cart = []; let currentCategory = 'all'; document.addEventListener('DOMContentLoaded', () => { renderMenu(); setupCategoryLinks(); updateCartUI(); }); function setupCategoryLinks() { const links = document.querySelectorAll('.nav-link'); links.forEach(link => { link.addEventListener('click', (e) => { e.preventDefault(); links.forEach(l => l.classList.remove('active')); link.classList.add('active'); currentCategory = link.dataset.category; renderMenu(); }); }); } function renderMenu() { const menuGrid = document.getElementById('menuGrid'); const filteredItems = currentCategory === 'all' ? menuItems : menuItems.filter(item => item.category === currentCategory); menuGrid.innerHTML = filteredItems.map(item => `<div class="menu-card"><div class="card-image">${item.emoji}</div><div class="card-content"><h3 class="card-title">${item.name}</h3><p class="card-description">${item.description}</p><div class="card-footer"><span class="card-price">$${item.price}</span><button class="add-btn" onclick="addToCart(${item.id})">加入購物車</button></div></div></div>`).join(''); } function addToCart(itemId) { const item = menuItems.find(i => i.id === itemId); if (!item) return; const existingItem = cart.find(i => i.id === itemId); if (existingItem) { existingItem.quantity++; } else { cart.push({...item, quantity: 1}); } updateCartUI(); showToast(`已加入 ${item.name}`); } function updateCartQuantity(itemId, change) { const item = cart.find(i => i.id === itemId); if (!item) return; item.quantity += change; if (item.quantity <= 0) { removeFromCart(itemId); } else { updateCartUI(); } } function removeFromCart(itemId) { cart = cart.filter(i => i.id !== itemId); updateCartUI(); } function getCartTotal() { return cart.reduce((sum, item) => sum + item.price * item.quantity, 0); } function getCartCount() { return cart.reduce((sum, item) => sum + item.quantity, 0); } function updateCartUI() { const cartCount = document.getElementById('cartCount'); const cartItems = document.getElementById('cartItems'); const cartTotal = document.getElementById('cartTotal'); const checkoutBtn = document.querySelector('.checkout-btn'); cartCount.textContent = getCartCount(); if (cart.length === 0) { cartItems.innerHTML = '<div class="cart-empty"><div class="cart-empty-icon">🛒</div><p>購物車是空的</p></div>'; checkoutBtn.disabled = true; } else { cartItems.innerHTML = cart.map(item => `<div class="cart-item"><div class="cart-item-image">${item.emoji}</div><div class="cart-item-info"><div class="cart-item-name">${item.name}</div><div class="cart-item-price">$${item.price}</div><div class="cart-item-controls"><button class="qty-btn" onclick="updateCartQuantity(${item.id}, -1)">-</button><span class="cart-item-qty">${item.quantity}</span><button class="qty-btn" onclick="updateCartQuantity(${item.id}, 1)">+</button><button class="remove-btn" onclick="removeFromCart(${item.id})">🗑️</button></div></div></div>`).join(''); checkoutBtn.disabled = false; } cartTotal.textContent = `$${getCartTotal()}`; } function toggleCart() { const overlay = document.getElementById('cartOverlay'); const sidebar = document.getElementById('cartSidebar'); overlay.classList.toggle('show'); sidebar.classList.toggle('show'); } function showCheckout() { if (cart.length === 0) return; toggleCart(); const orderSummary = document.getElementById('orderSummary'); orderSummary.innerHTML = cart.map(item => `<div class="summary-item"><span>${item.name} x ${item.quantity}</span><span>$${item.price * item.quantity}</span></div>`).join(''); document.getElementById('summaryTotal').textContent = `$${getCartTotal()}`; document.getElementById('checkoutModal').classList.add('show'); } function closeCheckout() { document.getElementById('checkoutModal').classList.remove('show'); } function submitOrder(e) { e.preventDefault(); const orderNumber = 'ORD' + Date.now().toString().slice(-8); closeCheckout(); document.getElementById('orderNumber').textContent = orderNumber; document.getElementById('successModal').classList.add('show'); cart = []; updateCartUI(); document.getElementById('checkoutForm').reset(); } function closeSuccess() { document.getElementById('successModal').classList.remove('show'); } function showToast(message) { const toast = document.createElement('div'); toast.style.cssText = 'position: fixed; bottom: 30px; left: 50%; transform: translateX(-50%); background: linear-gradient(135deg, #667eea, #764ba2); color: white; padding: 15px 30px; border-radius: 30px; box-shadow: 0 5px 20px rgba(0, 0, 0, 0.3); z-index: 1000; animation: toastIn 0.3s ease, toastOut 0.3s ease 2s forwards;'; toast.textContent = message; if (!document.getElementById('toastStyles')) { const style = document.createElement('style'); style.id = 'toastStyles'; style.textContent = '@keyframes toastIn { from { opacity: 0; transform: translateX(-50%) translateY(20px); } to { opacity: 1; transform: translateX(-50%) translateY(0); } } @keyframes toastOut { from { opacity: 1; transform: translateX(-50%) translateY(0); } to { opacity: 0; transform: translateX(-50%) translateY(-20px); } }'; document.head.appendChild(style); } document.body.appendChild(toast); setTimeout(() => toast.remove(), 2300); }
